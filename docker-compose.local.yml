services:
  reader:
    environment:
      STREAM_SERVER: ${LOCAL_STREAM_SERVER:-http://192.168.1.174:3005}
      NEMOTRON_ASR_URL: ${LOCAL_NEMOTRON_ASR_URL:-}
    ports:
      - "${READER_PORT:-3000}:3000"
